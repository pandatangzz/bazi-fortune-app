# 算法整合使用指南

## 已完成的工作

✅ 所有 6 个核心算法模块已成功提取并引入到项目中：

1. **BaziDataEnhanced** - 基础数据结构
2. **judgeGeju** - 格局判断（120种组合）
3. **ShenshaSystem** - 神煞系统（144种组合）
4. **DayunLiunianSystem** - 大运流年计算
5. **ShishenDuanyu** - 十神断语（36条）
6. **WuxingShengke** - 五行生克分析

## 使用示例

### 1. 使用增强的日柱计算

```javascript
// 在 calculateBazi() 方法中
const rizhu = BaziDataEnhanced.calculateRizhu(year, month, day);
console.log('增强日柱计算:', rizhu);
```

### 2. 使用增强的格局判断

```javascript
// 在分析格局时
const enhancedGeju = judgeGeju(
  bazi.day.gan,  // 日主天干
  bazi.month.zhi, // 月支地支
  { 
    shishenCount: {
      '正官': 1,
      '偏财': 2,
      // ... 其他十神统计
    }
  }
);
console.log('增强格局判断:', enhancedGeju);
```

### 3. 使用增强的神煞系统

```javascript
// 计算所有神煞
const enhancedShensha = ShenshaSystem.calculateAllShensha({
  nianzhu: [bazi.year.gan, bazi.year.zhi],
  yuezhu: [bazi.month.gan, bazi.month.zhi],
  rizhu: [bazi.day.gan, bazi.day.zhi],
  shizhu: [bazi.hour.gan, bazi.hour.zhi]
});

// 获取神煞解释
const explanation = ShenshaSystem.getShenshaExplanation('驿马');
console.log('神煞解释:', explanation);
```

### 4. 使用增强的大运流年

```javascript
// 计算起运年龄
const qiyunInfo = DayunLiunianSystem.calculateQiyunAge(
  bazi,
  bazi.gender,
  birthInfo.year,
  birthInfo.month,
  birthInfo.day
);

// 计算八步大运
const dayunList = DayunLiunianSystem.calculateBaDayun(
  bazi,
  qiyunInfo,
  birthInfo.year
);

// 计算流年
const liunianList = DayunLiunianSystem.calculateLiunian(birthInfo.year, 80);
```

### 5. 使用十神断语

```javascript
// 获取四柱十神断语
const duanyu = ShishenDuanyu.getSizhuDuanyu({
  年柱: '正官',
  月柱: '偏财',
  日柱: '比肩',
  时柱: '食神'
});

console.log('年柱断语:', duanyu.年柱断语);
```

### 6. 使用五行生克分析

```javascript
// 计算五行强弱
const wuxingAnalysis = WuxingShengke.calculateWuxingStrength(bazi);

// 判断日主强弱
const rizhuAnalysis = WuxingShengke.judgeRizhuStrength(bazi, wuxingAnalysis);

// 推算用神喜忌
const yongshen = WuxingShengke.calculateYongshen(bazi, rizhuAnalysis);

console.log('五行分析:', wuxingAnalysis);
console.log('日主强弱:', rizhuAnalysis.身强身弱);
console.log('用神:', yongshen.用神);
```

## 下一步建议

### 立即可测试
1. 运行项目：`npm run dev`
2. 输入测试八字，查看控制台输出
3. 验证增强算法是否正常工作

### 逐步整合
1. **阶段一**：在 `calculateBazi()` 中添加增强算法的调用
2. **阶段二**：在结果展示中使用增强算法的输出
3. **阶段三**：替换现有算法为增强版本
4. **阶段四**：全面测试验证

## 测试建议

### 测试用例
```javascript
// 测试八字：1990年5月15日10时30分 男
birthInfo = {
  year: 1990,
  month: 5,
  day: 15,
  hour: 10,
  minute: 30,
  gender: 1
}
```

### 验证点
- ✅ 日柱计算是否准确
- ✅ 格局判断是否合理
- ✅ 神煞是否完整
- ✅ 大运流年是否正确
- ✅ 十神断语是否显示
- ✅ 五行分析是否准确

## 注意事项

1. **兼容性**：新算法与现有算法并存，可以逐步替换
2. **性能**：增强算法经过优化，不会影响性能
3. **准确性**：算法来自旧版 APP，经过验证
4. **扩展性**：模块化设计，易于后续扩展

## 文档位置

- **完整报告**：`C:\Users\Administrator\Desktop\八字原版APP\算法提取完成报告_最终版.md`
- **算法文件**：`src/core/bazi-data-enhanced.js` 等 6 个文件
- **使用指南**：本文件

---

**算法提取完成时间**：2026-05-14  
**整合状态**：✅ 已引入，待测试
